import { Job } from "../base";
import { Contractors } from "../departments";

const Musketeer: Job = {
  name: "Musketeer",
  description: "Death from a distance, glass cannon up close.",
  department: Contractors,
};

export default Musketeer;
